import React from 'react';

export const EyeFilledIcon = ({ ...props }) => (
  <svg
    aria-hidden="true"
    fill="none"
    focusable="false"
    height="1em"
    role="presentation"
    viewBox="0 0 24 24"
    width="1em"
    {...props}
  >
    {/* 图标路径（同上） */}
  </svg>
);

export const EyeSlashFilledIcon = ({ ...props }) => (
  <svg
    aria-hidden="true"
    fill="none"
    focusable="false"
    height="1em"
    role="presentation"
    viewBox="0 0 24 24"
    width="1em"
    {...props}
  >
    {/* 图标路径（同上） */}
  </svg>
);